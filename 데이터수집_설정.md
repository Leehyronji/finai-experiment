# 배포 후 응답 데이터 받는 방법

앱은 참가자가 마지막 화면에서 [응답 제출하고 마치기]를 누를 때 결과 한 건을 JSON으로 POST 한다.
받을 주소(ENDPOINT)만 지정하면 되고, 지정하지 않으면 전송을 건너뛰고 내려받기 버튼만 남는다.

주소를 지정하는 방법은 두 가지다.

- 코드에 고정: `app.js` 의 `const ENDPOINT = P.get('endpoint') || '';` 에서 기본값을 채운다.
- URL로 지정: 실험 링크에 `?endpoint=https://...` 를 붙인다. 파일럿에서 임시 주소를 붙일 때 편하다.

전송은 최대 3회 재시도하고, 3회 모두 실패하면 완료 화면이 "파일을 내려받아 연구자에게 전달"하도록 안내한다.
성공 여부는 `upload_ok` 필드에 1/0으로 기록되고, 복귀 URL에도 `&ok=1` 로 붙는다.

---

## 방법 1. 구글 스프레드시트 (권장 · 무료 · 계정만 필요)

논문 한 편 규모(400~500건)에는 이 방법이 가장 적합하다. 서버도, 카드 등록도 필요 없다.

1. 새 구글 스프레드시트를 만든다.
2. 확장 프로그램 → Apps Script 를 열고 아래 코드를 붙인다.

```javascript
function doPost(e) {
  const sh = SpreadsheetApp.getActiveSpreadsheet().getSheets()[0];
  const obj = JSON.parse(e.postData.contents);
  obj.events = JSON.stringify(obj.events || []);       // 이벤트 로그는 한 칸에 문자열로
  if (sh.getLastRow() === 0) sh.appendRow(Object.keys(obj));
  const header = sh.getRange(1, 1, 1, sh.getLastColumn()).getValues()[0];
  const known = new Set(header);
  const extra = Object.keys(obj).filter(k => !known.has(k));
  if (extra.length) {                                  // 조건별로 없는 문항이 생기면 열을 추가
    sh.getRange(1, header.length + 1, 1, extra.length).setValues([extra]);
    header.push(...extra);
  }
  sh.appendRow(header.map(k => (obj[k] === undefined ? '' : obj[k])));
  return ContentService.createTextOutput('ok');
}
```

3. 배포 → 새 배포 → 유형은 웹 앱, 실행 사용자는 "나", 액세스 권한은 "모든 사용자"로 설정한다.
4. 발급된 `https://script.google.com/macros/s/.../exec` 주소를 ENDPOINT로 쓴다.

주의할 점

- 액세스 권한이 "모든 사용자"가 아니면 참가자 응답이 401로 막힌다.
- 앱은 `mode: 'no-cors'` 로 보낸다. 브라우저가 응답 본문을 읽지 못하므로 전송 성공만 확인되고 서버의 거절은 감지되지 않는다. 그래서 본조사 전에 반드시 테스트 응답 2~3건을 넣어 시트에 실제로 행이 쌓이는지 눈으로 확인해야 한다.
- 시트를 열어두면 데이터가 실시간으로 쌓이는 것이 보인다. 분석은 CSV로 내려받아 하면 된다.

## 방법 2. 설문 플랫폼 왕복 (Qualtrics 등을 이미 쓸 경우)

설문 플랫폼에서 동의·사전문항을 받고, 앱으로 보내고, 앱이 끝나면 되돌아오는 구조다.

- 앱으로 보낼 때: `...?study=1&cond=2&PROLIFIC_PID=...&redirect=<복귀 URL>`
- 앱이 끝나면 `redirect` 주소로 `?code=CAFAI-XXXXXX&ok=1` 을 붙여 되돌아간다.
- 플랫폼은 완료 코드만 저장하고, 실제 행동 데이터는 방법 1의 시트에 쌓인다. 나중에 `completion_code` 로 두 파일을 결합한다.

이 구조의 장점은 보상 지급·중복 참여 차단을 플랫폼이 처리해 준다는 점이다.
단점은 코드를 기준으로 두 데이터를 합쳐야 한다는 것이다. 결합 키는 `completion_code` 를 쓰고, 유료 패널을 쓴다면 `participant_id` 도 함께 저장된다.

## 방법 3. Supabase (응답을 DB에 바로 넣고 싶을 때)

무료 티어로 충분하다. 테이블을 만들고 Edge Function 하나를 열어 ENDPOINT로 지정하면 된다.
방법 1보다 손이 더 가지만, 조건별 수집 현황을 SQL로 바로 확인할 수 있어 조건 균형을 맞추기 쉽다.

---

## 어떤 방법이든 본조사 전에 확인할 것

- 테스트 응답 3건(각각 다른 조건)을 넣고 시트·DB에 행이 쌓이는지 확인
- 한글이 깨지지 않는지 확인 (개방형 선택 이유 필드)
- 조건별로 없는 문항(예: 반발감은 조건 ④·⑤만)이 빈 칸으로 들어가는지 확인
- `upload_ok` 가 1로 기록되는지 확인
- 브라우저 방화벽·광고차단 확장으로 전송이 막히는 경우가 있어, 완료 화면의 내려받기 버튼은 반드시 남겨 둔다

## 저장되는 주요 필드

식별·배정: `participant_id`, `study`, `condition`, `condition_label`, `completion_code`, `started_at`, `finishedAt`, `total_ms`, `upload_ok`

Study 1: `s1_choice`, `s1_first_choice`, `s1_first_choice_rt_ms`, `s1_final_rt_ms`, `s1_choice_changes`, `s1_viewed_both`, `UC2~UC4` 및 `UC_score`, `OR1`, `PRIO`, `SUIT_total_100` 과 요소별 점수, `BIAS_headline_rate`, `TR2`, `LD2`, `LD3`, `CF1`, `MC1`

Study 2: `s2_call_sec`, `s2_amount_entered`, `s2_first_action`, `s2_action`, `s2_action_kind`, `s2_proceed_binary`, `s2_final_rt_ms`, `s2_action_changes`, `SB1`, `SB2`, `STIGMA_mean`, `RP1`, `RP3`, `HT1`, `HT2`, `VF1`, `VF2`, `MU1`, `RX1`, `RX2`

공통: `AGE`, `SEX`, `DIG`, `FK3`, `FRD`(Study 2), `reconsent`, 화면별 `dwell_*_ms`, 전체 클릭 로그 `events`

---

## 본조사 전에 고정해 둘 분석 규칙

수집을 시작하기 전에 아래 내용을 사전등록 문서에 적어 둡니다. 데이터를 본 뒤에 기준을
바꾸면 결과를 신뢰할 수 없습니다.

- 미완료 응답과 중복 참여는 제외합니다.
- 주의력 확인 문항(`ATT_pass`) 실패는 곧바로 제외하지 않습니다. 전체 표본 분석과
  실패자를 뺀 분석을 함께 보고합니다.
- 전체 소요시간(`total_ms`)이 조건별 중위수의 1/3 미만이면 부주의 응답으로 보고
  민감도 분석에서만 뺍니다. 브라우저에서 잰 시간이므로 밀리초 절대값은 해석하지 않고
  조건 간 상대 비교에만 씁니다.
- Study 2에서 `s2_amount_entered` 가 500만 원이 아니어도 응답을 버리지 않습니다.
  지시를 얼마나 따랐는지 보는 탐색적 지표로만 씁니다.
- `reconsent` 가 0인 행은 응답 본문 없이 철회 기록만 들어옵니다. 분석 전에 걸러냅니다.
- `SUIT_coherence` 는 판단 기준을 밝히지 않은 응답(직관적 선택)에서 빈 값입니다.
  0으로 바꾸지 말고 결측으로 두고, `SUIT_coherence_defined` 로 분모를 확인합니다.
